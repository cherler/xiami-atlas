# 该你看了 · 2026-09-11

**43 条要你拍板**，分 3 摞。每摞都指向它原本在的文件 —— **这里只汇总，不下判断**。

> **动手在审阅台：http://127.0.0.1:8230**（`npm run station` 开）。
> 这份文件是存档，看的是同一摞东西；判断、重跑、写回都在那边点，不要在两处各做一半。

## 开源项目仓库数字：会改变结论的变化 3

星数、停更月数是**会过期的事实**。「在维护」变成「停更 12 个月」之后，卡上的标签、`unfit` 里的判断、要不要继续收录，全都得跟着重想 —— 页面却照写不误。

→ 确认后跑 `npm run repos` 写回（审阅台上有按钮），再上线。 明细在 data/fresh-report.md

## README 漂移：上游改过话 39

我们对开源项目的判断是从**当时那一版 README** 读出来的。上游改了口径，页面上那句引用就成了假话，而没有任何人会收到通知。

→ 逐条核页面上引它的判断；核完 `node scripts/fetch-readme.mjs <项目 id>` 更新快照。 明细在 data/fresh-report.md

## 管线「extract」这一步被跳过，已经连着 34 天

**一个报告成功却什么都没做的定时任务，比一个明着失败的更糟。** 整步没跑不会让任何一份报告变红 —— 那几份报告本来就只报它们自己看得见的东西。

→ 原因：没有 MINIMAX_API_KEY —— 密钥要写进仓库根的 .env

---

## 机器状态 · 不用你判断（72）

> 这几摞是**机器自己的账**：够不着的源、等上游放开的重核、复查工具自查不过的结构性错配。
> 留着是为了不假装「都好」，但**没有一条要你拍板**。要根治是改代码，不是你去看。

- **抓不到：5 个源** —— 长期抓不到本身是信息：那家没有可 diff 的公开入口，也解释了它名下的格子为什么最差。
- **待重核 55 格（ltx 9 · gpt-image 2 · gemini-text 11 · glm 12 · grok 14 · hunyuan3d 3 · hallo2 4）** —— 厂商的能力页常常落后于它自己的模型清单。
- **引文回查：12 个源上的原句对不上了** —— 失败不等于引文错。

---

> 「该你拍板」这一摞才受 §59.3「人每周看的 ≤20」的负反馈管；机器状态不算在内。
> 红色永远不放宽 —— 那批错了会污染整张图。
